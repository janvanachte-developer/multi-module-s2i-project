#!/bin/bash
set -eo pipefail

echo
echo "Using module-specific S2I script"
echo

if [ x"$DATA_FILE" != "x" ]; then
    mkdir -p $DATA_FILE
    cp /tmp/src/fis/src/main/resources/data/country_data.csv $DATA_FILE
fi

exec /usr/local/s2i/run
